@model IEnumerable<DesafioMVC.Models.Business>
@{
    Layout = "_LayoutAdmin";
}
<div class="card">
    <div class="card-header card-header-primary">
        <div class="row">
            <div class="col">
                <h2 class="card-title">Negócios</h2> 
            </div>
            <div class="col align-self-center text-right">
                <a asp-controller="Admin" asp-action="NewBusiness" 
                    class="btn btn-secondary" 
                >
                    Criar novo negócio
                </a>
            </div>
        </div>
    </div>
    <div class="card-body">
        <div class="table-responsive">
            <table class="table">
                <thead class="text-primary">
                    <tr>
                        <th>Id</th>
                        <th>Nome</th>
                        <th>Ações</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var business in Model) {
                        <tr>
                            <td>@business.Id</td>
                            <td>@business.Name</td>
                            <td>
                                <a class="btn btn-success" href="/Admin/EditBusiness/@business.Id">Editar</a> 

                                <form asp-controller="Businesses" asp-action="Delete" method="POST" style="display: inline;">
                                    <input type="hidden" name="id" id="id" value="@business.Id">
                                    <button class="btn btn-danger">Deletar</button>
                                </form>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>